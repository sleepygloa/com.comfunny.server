<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.comfunny.server.proj.sd.repository.ClientDao">


    <select id="selectClientCmbList" resultType="HMap" >
        SELECT
            CLIENT_CD  VALUE
             , CLIENT_NM  NAME
        FROM tb_ms_client
        WHERE 1=1
          AND BIZ_CD = #{s_bizCd}
    </select>

    <select id="selectClientList" resultType="HMap" parameterType="HMap">
        SELECT /*[ClientDao.xml][selectClientList] 고객사 데이터들을 조회 */
               BIZ_CD
             , CLIENT_CD
             , CLIENT_NM
             , BIZ_NO
             , BIZ_NM
             , CEO_NM
             , POST_NO
             , BASIC_ADDR
             , DETAIL_ADDR
             , BIZ_TP
             , BIZ_KND
             , TEL_NO
             , FAX_NO
             , COUNTRY_CD
             , CITY_CD
             , USER_COL1
             , USER_COL2
             , USER_COL3
             , USER_COL4
             , USER_COL5
             , REMARK
             , IN_DT
             , IN_USER_ID
             , UP_DT
             , UP_USER_ID
          FROM tb_ms_client
         WHERE 1=1
          <if test="clientNm != null and clientNm != ''">
           AND (
                CLIENT_CD LIKE '%'#{clientNm}'%'
             OR CLIENT_NM LIKE '%'#{clientNm}'%'
              )
          </if>
    </select>

</mapper>
